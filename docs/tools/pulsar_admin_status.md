#### pulsar_admin_status

Check Pulsar broker or proxy service status via the `/status.html` endpoint.

- Returns the raw service status text, equivalent to `pulsarctl status check`.
- Requires super-user permissions.
